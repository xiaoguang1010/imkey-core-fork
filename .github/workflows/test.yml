name: Test

on:
  push:
    tags:
    - '*'

jobs:
  build:

    runs-on: macos-12

    steps:
#     - uses: actions/checkout@v3
    - name: Install opssl
      run: |
        pwd
        echo aaa
        ls
        tag=${{ github.ref }}
        echo $tag
        echo ${tag: 10}
