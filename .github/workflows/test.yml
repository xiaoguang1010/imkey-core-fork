name: Test

on:
  push:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: macos-12

    steps:
    - uses: actions/checkout@v3
    
#     - name: Setup Android SDK
#       uses: android-actions/setup-android@v2

    - name: Install Rust
      run: |
        rustup install nightly-2022-12-06
        echo ccccccc
        rustup target add aarch64-apple-ios x86_64-apple-ios
        rustup show
        echo bbbbbb
        
        
