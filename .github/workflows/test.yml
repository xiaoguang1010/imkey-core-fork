name: Test

on:
  push:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: macos-12

    steps:
    - uses: actions/checkout@v3
    
#     - name: Setup Android SDK
#       uses: android-actions/setup-android@v2

    - name: Install Rust
      run: |
        rustup install 1.47.0
        rustup toolchain install nightly-2020-10-07
        rustup default nightly-2020-10-07
        echo aaaaaaa
        rustup target add aarch64-apple-ios x86_64-apple-ios
        rustup show
        echo bbbbbb
        cargo install cargo-lipo
        
