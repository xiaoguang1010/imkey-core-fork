name: Test

on:
  push:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: macos-12

    steps:
#     - uses: actions/checkout@v3
    - name: Install opssl
      run: |
        pwd
        echo aaa
        cd ${{github.workspace}}
        pwd
        ls -a
        tag=${{ github.ref }}
        echo $tag
        echo ${tag: 10}
        zip -q -r tttttttt.zip . ./mobile-sdk
        ls
        
